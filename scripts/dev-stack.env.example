# scripts/dev-stack.env — biến dùng bởi scripts/dev.sh
# Copy thành dev-stack.env và sửa nếu cần (file này không bị commit).

# Port FastAPI backend
PORT_API=8000

# Port Vite dev server
PORT_WEB=8080

# Bật log request backend (1 = on, 0 = off)
API_DEBUG=0
